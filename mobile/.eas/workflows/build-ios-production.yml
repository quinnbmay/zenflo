name: Build iOS Production
on:
  push:
    branches:
      - main
    paths:
      - 'mobile/**'
      - '!mobile/README.md'
      - '!mobile/**/*.md'
  workflow_dispatch: {}

jobs:
  build:
    name: Build iOS App
    type: build
    params:
      platform: ios
      profile: production
