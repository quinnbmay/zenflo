name = "zen-mode-mcp"
main = "src/index.ts"
compatibility_date = "2024-11-07"

# Custom domain routes
[[routes]]
pattern = "zen.zenflo.dev/*"
zone_name = "zenflo.dev"

# No environment variables needed - auth is per-request via secret key

[build]
command = ""

# Free tier usage limits
# - 100,000 requests/day
# - 10ms CPU time per request
# - 128MB memory
