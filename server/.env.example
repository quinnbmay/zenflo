# ZenFlo Server Configuration
# Copy this file to .env and update with your values

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:password@localhost:5432/zenflo-server

# Security
# Master secret for encryption (generate with: openssl rand -base64 32)
HANDY_MASTER_SECRET=your-secret-key-here

# Server Configuration
PORT=3005
NODE_ENV=development

# Metrics
METRICS_ENABLED=true
METRICS_PORT=9090

# Redis
# Optional - for caching and pub/sub
REDIS_URL=redis://localhost:6379

# GitHub OAuth Integration
# Register OAuth app at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URL=https://api.zenflo.dev/v1/connect/github/callback

# Expo Push Notifications
# Optional - for mobile push notifications
# Get token from: https://expo.dev
EXPO_ACCESS_TOKEN=your-expo-access-token

# Debug Logging (Development Only)
# NEVER enable in production - stores logs to .logs/ directory
DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING=false

# CORS Configuration
# Allowed origins are configured in sources/app/api/api.ts and socket.ts
# Default origins include:
#   - https://app.zenflo.dev (webapp)
#   - https://api.zenflo.dev (API)
#   - https://app.combinedmemory.com (legacy webapp)
#   - https://happy.combinedmemory.com (legacy API)
#   - http://localhost:* (development)
